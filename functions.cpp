using namespace std;

bool check_prime(unsigned a) { // Check if a number is prime
    if()
    bool isPrime = true;
    for(unsigned i = 2; i < a; i++) {
        if(a%i == 0) { isPrime = false; break; }
    }
    return isPrime;
}

bool check_prime(float a) {

}